# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=toeup
POSTGRES_PORT=5432
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/toeup"                          # Dùng cho Local (không Docker)

# API Configuration
PORT=8000
API_PORT=8000
WEB_PORT=3000

# Authentication (Google OAuth)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL="http://localhost:8000/auth/google/callback" # Dev
# GOOGLE_CALLBACK_URL="https://your-api-domain.com/auth/google/callback" # Prod

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key    

# Frontend & Backend URLs
FRONTEND_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:8000"

# Email Service (Mailjet / SMTP)
SMTP_HOST=in-v3.mailjet.com
SMTP_PORT=2525
SMTP_USER=your_mailjet_api_key
SMTP_PASS=your_mailjet_secret_key
SMTP_SECURE=false
SMTP_FROM='"FlashUp App" <your_verified_sender@example.com>'

# AI Features (Optional)
GEMINI_API_KEY=your_gemini_api_key